BiocManager::install("EnhancedVolcano")

BiocManager::install("miRNAtap")
#this doesn't seem to have target to miRNA info easily retrievable
#using below one instead

BiocManager::install("multiMiR")
